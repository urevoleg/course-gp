# Оптимизация производительности запросов

- 💣 [Практическое задание 1 (EXPLAIN)](https://github.com/urevoleg/course-gp/blob/main/chapter3/README.md#%EF%B8%8F-практическое-задание-1-получение-плана-запроса)
- 💣 [Практическое задание 2 (EXPLAIN ANALYZE)]()
- 💣 [Практическое задание 3 (partition)]()
- 💣 [Практическое задание 4 (partition manipuilation)]()

[👉 К оглавлению](https://github.com/urevoleg/course-gp/blob/main/chapter3/README.md#оптимизация-производительности-запросов)

## Как выполняются запросы в Greenplum

Сервис по раскуриванию планов запроса [link](https://demo-explain.tensor.ru/plan/)

**Неоптимальный запрос** — запрос, который требует для выполнения субъективно большего количества ресурсов (время, CPU, RAM, дисковое пространство), чем мог бы.

**Оптимизация запроса** — изменение SQL-запроса и контекста его выполнения (например, структур данных и настроек сессии) для ускорения выполнения и
уменьшения затраченных ресурсов без изменения результатов и аппаратной конфигурации СУБД.

**Оптимальный запрос** — запрос, любая оптимизация которого не приведёт к статистически значимому уменьшению времени работы или утилизации ресурсов кластера.

**Планировщик запросов** — подпрограмма ядра Greenplum, которая по тексту запуска строит последовательность действий. 
Подпрограмма выполняется перед каждым запуском SQL-запроса. Эти действия выполняются в СУБД для получения требуемого результата.

Для формирования плана запроса используется:
* Cтруктура таблиц и распределение данных в них по кластеру.
* Наличие или отсутствие индексов.
* Особые метаданные — табличную статистику.

**План запроса** — автоматически построенная планировщиком по SQL-запросу последовательность операций в СУБД, запуск которой и есть выполнение этого запроса.

Чтобы получить план запроса необходимо перед запросом доьбавить ключевое слово `EXPLAIN`:

```sql
EXPLAIN SELECT * FROM table JOIN dict USING (id);
```

### ✍️ Практическое задание 1: получение плана запроса

1. Создайте две новые таблицы table1 и table2 в схеме public.

```sql
DROP TABLE IF EXISTS table1;
CREATE TABLE table1 (id int, dt date) 
DISTRIBUTED BY (id);

DROP TABLE IF EXISTS table2;
CREATE TABLE table2 (id int, val text) 
DISTRIBUTED BY (id);
```

2. Заполняем данными

```sql
INSERT INTO table1 (id, dt)
  (
    SELECT 
      id, 
      '2023-06-01'::date + interval '1 month' * (id%10) 
    FROM generate_series(1,100) as id
  );

INSERT INTO table2 (id, val)
  (
    SELECT 
      id, 
      'ab'::text || chr(ascii('a') + mod(id,3))::text 
    FROM generate_series(1,100) as id
  );
```

3. Смотрим план запроса

```sql
EXPLAIN 
SELECT *
FROM 
  (SELECT * FROM table1 WHERE table1.dt='2023-08-01') AS t1
  INNER JOIN
  (SELECT * FROM table2) AS t2
    ON t1.id = t2.id
WHERE t2.val='abc';
```

![explain_example.png](..%2Fimg%2Fexplain_example.png)

[👉 К оглавлению](https://github.com/urevoleg/course-gp/blob/main/chapter3/README.md#оптимизация-производительности-запросов)

-----------------------------------

Допинфа в плане запроса:
- **Стоимость, или затраты (cost)**, — абстрактная величина в условных «попугаях», показывающая сложность или затраты ресурсов выполнения оператора.
- **Ориентировочное количество записей**, возвращённых оператором (rows), — приблизительная оценка количества переданных оператором записей.
- **Размер записей**, возвращаемых оператором (width), — размер одной записи в байтах.
- **(Опционально) Дополнительные свойства оператора** (параметры оператора) — атрибуты оператора, детализирующие характер его выполнения: например, условия фильтрации или ключи объединения таблиц.
- **(Для некоторых операторов) Срез, или слайс (slice)**, — часть плана запроса, которая выполняется независимо от всех остальных его частей.

При использовании `EXPLAIN` - сам запрос не выполняется, чтобы получить реальный план, по которомы выполнен запрос используется
`EXPLAIN ANALYZE` - его вывод содержит дополнительную информацию

----------------------------------
### ✍️ Практическое задание 2: получение плана фактического выполнения запроса

1-3 Аналогичны предыдущим

4. Фактический план запроса

```sql
EXPLAIN ANALYZE
SELECT *
FROM 
  (SELECT * FROM table1 WHERE table1.dt='2023-08-01') AS t1
  INNER JOIN
  (SELECT * FROM table2) AS t2
    ON t1.id = t2.id
WHERE t2.val='abc';
```

![explain_analyze_example.png](..%2Fimg%2Fexplain_analyze_example.png)

[👉 К оглавлению](https://github.com/urevoleg/course-gp/blob/main/chapter3/README.md#оптимизация-производительности-запросов)

---------------------------------

Видим что появилась дополнительная информацию и фактическом выполнении каждой стадии плана:
- **actual time** — время работы оператора в миллисекундах. Всегда содержит два значения, разделённые двумя точками `..`: время возврата оператором первой записи и общее время работы оператора. Включает время работы всех нижележащих операторов.
- **rows** — количество записей на одном сегменте (кроме оператора Gather Motion).
- **loops** — количество итераций выполнения оператора (оператор может выполняться более одного раза).
- Дополнительные характеристики выполнения оператора (параметры оператора) — например, размер сгенерированной хеш-таблицы или объём временных данных, записанных на диск во время работы оператора.

Также добавилась информацию о всем запросе:
- Planning time
- Executor memory
- Memory Used
- Execution time

Результаты EXPLAIN и EXPLAIN ANALYZE всегда будут совпадать - если между ними не выполнялось никаких операций по изменению 
структуры, наполнения или табличной статистики и настроек СУБД.

----------------------------

## Базовые элементы плана запроса. Анализ плана выполнения

Таблица **Часто встречающиеся операторы Greenplum**

![freq_operators_gp.png](..%2Fimg%2Ffreq_operators_gp.png)

### Операторы доступа к данным: 
1. SeqScan, AppendOnlyScan, ColumnScan:
 - **SeqScan** - для HEAP таблиц
 - **AppendOnlyScan** - только для AppendOptimized таблиц со строковой ориентацией
 - **ColumnScan** - для колоночной ориентации
2. **ExternalScan** - для внешних таблиц
3. IndexScan, BitmapIndexScan:
 - **IndexScan** - сканирование подходящего индекса, он отображается в параметре using
 - **BitmapIndexScan**
4. **DynamicSeqScan** - чтение записей из нескольких партиций одной таблицы. Позволяет читать только нужные партиции, но иногда 
сходит с ума и предлагает читать всю таблицу, такой вот у него минус
5. **SharedScan (+Materialize)** - присутствует только если есть CTE, испольуемые более 1 раза

### Операторы преобразования данных
Из операторов преобразования данных интересны JOIN:
1. **HashJoin** - самый быстрый алгоритм соединения. По дефолту

Основы логики:
- считаем правая таблица - внутренняя, левая таблица - внешняя
- внутренняя таблица проходлит через оператор HASH -> hash table with key = join key
- в качестве внутренней таблицы планировщик всегда выбирает с меньшим кол-вом записей, узнает это он из статистики
- внешняя таблицы сканируется, для каждой записи для ключа key высчитывается hash тем же алгоритмом и из правой для таких же
 hash проверяется реальное равенство значений, при равенстве исходных значений результаты внешней и внутренней таблиц сохраняются в результат

Есть небольшие отличие между Hash Left Join и Hash Right Join. Для Right Join выполняется дополнительное сканирование внутренней таблицы
и все строки без специального флага добавляются в результат

- Во время выполнения Hash Join большая часть данных и внутренней, и внешней таблиц сохраняются в памяти.

2. **MergeJoin** - объединение предварительно отсортированных таблиц

- В GP сортировка записей не предусмотрена, поэтому такой тип соединения встречается редко
- Для корректной работы Merge Join требуется предварительная сортировка одной или обеих входных таблиц по ключу join. Сортировка реализуется только оператором Sort, поэтому при построении входных данных для Merge Join промежуточные результаты сортировки сохраняются в памяти. 
При этом объём промежуточных данных будет меньше, чем у Hash Join.

3. **Nested Loop Join** - соединение методом вложенных циклов

- может выполнятся с ОГРОМНЫМ кол-вом записей в таблицах, тк не требуется большого кол-ва памяти
- может быть оптимальным, но не обязан: одна таблица >>>> другой (типа таблица <-> словарь) или у одной из таблиц есть подходящий индекс
- только этот тип соединения может реализовать отличную от равенства ключей логику например больше или меньше или между
- 😈 в общем случае самый медленный, сильно утилизирует CPU
- 😈 не умеет FULL JOIN -> будет ошибка


Агрегация данных входной таблицы реализуется при помощи:
- **HashAggregate** (GROUP BY, window func and DISTINCT) - реализует логику hash таблиц, недостаток промежуточный результат сохраняется в память
- **GroupAggregate** (GROUP BY, DISTINCT) - требует сортировки входных данных
- **WindowAgg** (window func) - требует сортировки входных данных

### Операторы перемещения данных (семейство Motion)

Тк данные распределены между отдельными сегментами, то иногда их требуется перемещать, для этого используются 3 оператора:
1. **Redistribute Motion**: Перераспределяет записи между сегментами по новому ключу. Используется перед операторами соединения и агрегации.
Записи перемещаются перед соединением, чтобы поместить записи из двух таблиц с одинаковым ключом на один сегмент. 
В этом случае join может выполниться локально на сегменте.
2. **Broadcast Motion**: Размножает записи на все сегменты
3. **Gather Motion**: Собирает записи с сегментов на мастер.

😈 При перемещении данных между сегментами через Motion-операторы связанные с данными индексы не перемещаются и перестают использоваться для ускорения запроса.
Эта особенность вместе с частым появлением Motion-операторов в планах — причина редкого использования индексов в Greenplum.

### Служебные операторы

1. **Result**: добавляет ко входным данным результат вычисления функции
2. **Append**: применяется в двух случаях: **UNION\UNION ALL** и механизм **Partition Pruning**
3. **Sequence**: Последовательно выполняет входящие в него операторы и выдает результат последнего, ходит парой с Materialize
4. **Materialize**: используется только GPORCA в запросах с CTE (они выполняются только 1 раз, после считываются при помощи оператора SharedScan). 
После первого использования все остальные использования будут помечены _never executed_
5. **Partition Selector** - используется если есть WHERE, отбирает только те партиции, которые необходлимы - это и есть механизм **Partition Pruning**
Ходит парой с Sequence и Dynamic Seq Scan
6. **Hash** - ходит парой с HashJoin - строит Хеш-таблицу

--------------------------
## Накопление операторами промежуточных данных. Spill-файлы

Для хранения промежуточных результатов используется выделенная оператору память, иногда её перестаёт хватать и тогда промежуточные
результаты сбрасываются на диск - формируются spill-файлы

![spill_files.png](..%2Fimg%2Fspill_files.png)

Одной из задач оптимизации является — выявление факта создания спилл-файлов и определение возможных сценариев исправления ситуации.

В плане запроса сигнализирует о наличии spill файлы:
- отметка звездочкой
- наличие записи Work_mem ... wanted

Какие операторы могут приводить к созданию spill:
- Sort - запись Disk .... - промежуточные результаты сбрасываются на диск
- Hash - запись Wrote … to inner/outer workfile
- HashAggregate - поле overflows
- Materialize - в плане запросе не указывается явно создание spill файлов

## Факторы, влияющие на оптимальность плана запроса

![we_need_a_plan.png](..%2Fimg%2Fwe_need_a_plan.png)

Что использует планировщик:
- входная статистика и инфа (статистика по таблицам + наличие индексов, партиций, распределение данных по кластеру)
- наличие различных операторов в SQL (SELECT, OFFSET\LIMIT, GROUP BY и др)
- переписывание запроса (замена констант, подставление имен и др)
- построение списка операторов , которыми можно реализовать запрос
- перебор возможных комбинаций (это NP-трудная задача, поэтому планировщик выбирает почти оптимальный план, тк время на поиск решения не может быть бесконечным)

[Применение машинного обучения для увеличения производительности PostgreSQL](https://habr.com/ru/companies/postgrespro/articles/273199/) - интересная статья про ML в оптимизации.
Внутри есть ссылки:
- [статья](https://arxiv.org/abs/1711.08330)
- [код](https://github.com/postgrespro/aqo)
- конференции [раз](https://highload.ru/2016/abstracts/2335.html), [два](https://pgconf.ru/en/2017/93634), [три](https://pgconf.ru/en/2017/93634)

### Отсутствующая или неактуальная статистика

Обновить статистику можно:
- ручками `ANALYZE`
- автоматически - определяется настройками сервера
- в Managed Service - при техническом обслуживании

### Неоптимальная физическая модель данных

#### Дистрибуция данных

Влияет на перенос данных между сегментами

#### ✍️ Практическое задание 3: перемещение данных между сегментами

1. Создаем таблицы

```sql
DROP TABLE IF EXISTS table1;
CREATE TABLE table1 (id int, dt date) 
DISTRIBUTED BY (id);

DROP TABLE IF EXISTS table2;
CREATE TABLE table2 (id int, val text) 
DISTRIBUTED BY (val);
```

2. Заполняем данными и обновление статистики ручками

```sql
INSERT INTO table1 (id, dt)
  (
    SELECT 
      id, 
      '2023-06-01'::date + interval '1 month' * (id%10) 
    FROM generate_series(1,1000000) as id
  );

INSERT INTO table2 (id, val)
  (
    SELECT 
      id, 
      'ab'::text || chr(ascii('a') + mod(id,3))::text 
    FROM generate_series(1,1000000) as id
 );

ANALYZE table1;
ANALYZE table2;
```

3. План запроса

```sql
EXPLAIN (ANALYZE, COSTS OFF)
SELECT *
FROM 
  table1 INNER JOIN table2 ON table1.id = table2.id; 
```

![explain_3_1.png](..%2Fimg%2Fexplain_3_1.png)

**Отмечаем**: наличие Redistribute Motion + Work_mem

4. Изменяем дистрибуцию и делаем реогрганизацию

```sql
ALTER TABLE table2 SET
WITH (reorganize=true)
DISTRIBUTED BY (id);

EXPLAIN (ANALYZE, COSTS OFF)
SELECT *
FROM 
  table1 INNER JOIN table2 ON table1.id = table2.id; 
```

![explain_3_2.png](..%2Fimg%2Fexplain_3_2.png)

После изменения дистрибуции второй таблицы на дистрибуцию по ключу - Redistribute Motion исключен

### ✍️ Практическое задание 4: динамическое исключение партиций

1. Создайте партицированную таблицу dpe_table и наполните её данными. В таблице должно быть пять партиций.

```sql
DROP TABLE IF EXISTS dpe_table;

CREATE TABLE dpe_table (id int, p_key int, dt date) 
DISTRIBUTED BY (id)
PARTITION BY LIST (p_key)
(
  DEFAULT PARTITION table4_pdef,
  PARTITION table4_p1 VALUES (1),
  PARTITION table4_p2 VALUES (2),
  PARTITION table4_p3 VALUES (3),
  PARTITION table4_p4 VALUES (4)
);

INSERT INTO dpe_table (id, p_key, dt)
  (
    SELECT 
      id, 
      id % 5 AS p_key,
      '2023-06-01'::date + interval '1 month' * (id%10) 
    FROM generate_series(1,10000000) AS id
  );

-- Не лишним будет убедиться, что статистика актуальна
ANALYZE dpe_table; 
```

2. Рассмотрите план запроса, полученный командой EXPLAIN ANALYZE. 
В условии WHERE должно быть указано поле партицирования, и к нему задан явный фильтр.

![explain_where_part.png](..%2Fimg%2Fexplain_where_part.png)

Видим: что прочитано 3 партиции из 5

3. Теперь попробуйте просканировать всю таблицу и сравнить время выполнения с полученным вами результатом. (непонятный момент)
4. На самом деле, механизм отбора партиций довольно хрупкий и корректно работает только с фильтрами, 
которые содержат ключ партицирования без каких-либо преобразований.

![explain_where_with_transform.png](..%2Fimg%2Fexplain_where_with_transform.png)

При использовании преобразований прочитаны все партиции, правда время запроса не изменилось

-----------------------------------

### ✍️ Практическое задание 5: индекс в запросах, содержащих фильтры к полям

1. Создайте пару таблиц big_table и small_table и наполните их данными. В одной таблице — 10 000 000 записей и 
три значащих столбца, в каждом из которых по 1000, 10 000 и порядка 6 400 000 уникальных записей. 
Во второй таблице — чуть больше 100 записей из первой таблицы.

```sql
DROP TABLE IF EXISTS big_table;
CREATE TABLE big_table (id, int1k, int10k, int10m) 
AS SELECT 
  id,
  floor(random() * 1000 + 1)::int AS int1k,
  floor(random() * 10000 + 1)::int AS int10k,
  floor(random() * 10000000 + 1)::int AS int10m
FROM generate_series(1,10000000) as id
DISTRIBUTED BY (id);
ANALYZE big_table;

DROP TABLE IF EXISTS small_table;
CREATE TABLE small_table (id, int1k, int10k)  
AS SELECT id, int1k, int10k FROM big_table OFFSET floor(random()*100) LIMIT floor(random()*100 + 100)
DISTRIBUTED BY (id);
ANALYZE small_table; 
```

2. План запроса

```sql
EXPLAIN ANALYZE 
  SELECT * 
  FROM 
    big_table bt 
      INNER JOIN 
    small_table st 
      ON bt.id=st.id AND bt.int1k=st.int1k AND bt.int10k=st.int10k;
```

![explain_with_index.png](..%2Fimg%2Fexplain_with_index.png)

Фиксируем время выполнения: 337мс


3. Добавляем индекс на join key и проверяем план запроса

```sql
DROP INDEX IF EXISTS idx_big_table_int10k_int1k;
CREATE INDEX idx_big_table_int10k_int1k ON big_table USING btree (int10k, int1k);

EXPLAIN ANALYZE 
  SELECT * 
  FROM 
    big_table bt 
      INNER JOIN 
    small_table st 
      ON bt.id=st.id AND bt.int1k=st.int1k AND bt.int10k=st.int10k;
```

План запроса:

![explain_with_index_1.png](..%2Fimg%2Fexplain_with_index_1.png)

Добавился IndexScan и и время выполнения запроса сократилось почти на 2 порядка: 4.2мс

----------------------------

## Проблемные конструкции в SQL-запросах

1. Селект всех полей, в то время как нужны только несколько - тк размер входящих строк получается большим - это может портить план запроса
2. Лишняя сортировка: часто в качестве отладки или тестирования добавляется используется сортировка, но после она уже не нужна, но остается и добавляет лишний оператор Sort в план запроса
3. DISTINCT - добавляет лишний этап сортировки или построения хэш-таблицы, что замедляется запрос
4. UNION/UNION ALL - UNION не явно выполняет  DISTINCT над набором UNION ALL. Также в GP эти операции выполняеются не очень эффективно из-за распределенного хранения

----------------------------

## Планировщики в Greenplum: Postgres и GPORCA

![gp_optimizer.png](..%2Fimg%2Fgp_optimizer.png)

### ✍️ Практическое задание 6: GPORCA vs Postgres

1. Создайте таблицу table1 и наполните её данными.

```sql
DROP TABLE IF EXISTS table1;
CREATE TABLE table1 (id int, dt date) 
DISTRIBUTED BY (id);

INSERT INTO table1 (id, dt)
  (
    SELECT 
      id, 
      '2023-06-01'::date + interval '1 month' * (id%10) 
    FROM generate_series(1,1000) as id
  );
ANALYZE table1;
```

2. Постройте план выполнения запроса с использованием планировщика Postgres

```sql
SET optimizer=off;
EXPLAIN ANALYZE 
  SELECT DISTINCT * FROM 
    (
      SELECT id, row_number() OVER (ORDER BY id) AS rn, dt 
      FROM table1
    ) t1;
```

![explain_pg_optimizer.png](..%2Fimg%2Fexplain_pg_optimizer.png)

3. Постройте план выполнения этого же запроса, но с помощью планировщика GPORCA

```sql
SET optimizer=on;
EXPLAIN ANALYZE 
  SELECT DISTINCT * FROM 
    (
      SELECT id, row_number() OVER (ORDER BY id) AS rn, dt 
      FROM table1
    ) t1;
```

![explain_gp_optimizer.png](..%2Fimg%2Fexplain_gp_optimizer.png)

План получился длинее, время выполнения дольше, время планирования дольше

---------------------------
## Настройки СУБД, влияющие на поведение планировщика или выделение памяти запросу

1. Настройки, влияющие на поведение планировщика
